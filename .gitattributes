# 统一换行符策略：
#   - 仓库内按 LF 存储（text=auto 自动归一化）；
#   - 检出 / 导出（含 git archive 打包）时强制 CRLF，
#     避免 Windows 下 .cmd 的 if(...) 块和 .ps1 因换行符出现解析差异。
* text=auto
*.ps1  text eol=crlf
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.md   text eol=crlf
*.json text eol=crlf
*.yml  text eol=crlf