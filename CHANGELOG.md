# 更新日志

本项目所有值得记录的改动都会写在这里。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，日期格式为 `YYYY-MM-DD`。

## [1.5.1] - 2026-09-12

这一版是 v1.5.0 的修补：修掉几处「看着正常、其实会误导人或者埋雷」的地方。功能与默认值都没有变化。

### 修复

- **安装自检不再误报失败**：v1.4.0 之后脚本在「能上网」时会直接退出、不写 `state.json`，而安装脚本是靠 `state.json` 里的 `RunCount` 增加来判断任务有没有真的跑起来。结果在联网正常的机器上，自检几乎必然报红色失败，还会把任务降级成直接调用 `powershell.exe`。现在同时看两条证据：`RunCount` 增加，**或者**计划任务自己的「最近一次运行时间」往后走，任意一条成立就算通过。
- **`state.json` 改为每次触发都刷新**：新增 `LastTrigger` 字段，`RunCount` 按触发次数累加。于是「脚本还活着吗」可以直接看 `state.json` 的修改时间（正常在 30 秒以内），不必再等下一次真实的 Portal 查询。注意这只写本地文件，**不产生任何网络请求**，不影响「能上网时零请求」的设计。
- **安装时提醒“脚本放在临时目录”**：如果主脚本位于桌面、下载或临时目录，安装脚本会明确警告——删掉那个文件夹之后，计划任务会变成每次启动都失败（返回码 `0x8007010B`）的死任务。这正是旧版 `D:\campus-network-auto-login` 被删掉之后实际发生的情况。
- **诊断报告会检查“任务指向的脚本还在不在”**：跟着隐藏启动器找到真正执行的主脚本，显示它是否存在、版本号，并在「起始目录不存在」「主脚本不存在」「脚本位于桌面/临时目录」时给出明确提示；`state.json` 超过 5 分钟没更新也会直接报警。
- **修正文档与诊断报告里的错别字**（“退化成都市按有网处理” → “退化为按有网处理”），README 里的“当前版本”从 1.1.0 更正为 1.5.1。
- **通用版本（`generic/`）默认 `RetryCount` 由 3 改为 1**：在一次运行里连打三次登录请求，正是 v1.1.0 被 Portal 限流、把正在用的会话顶掉的老毛病；下一次检查就在 1~2 分钟后，没有必要在一次运行里重试三次。

### 说明

- 不需要重新输入账号密码（`credential.xml` 与目录无关）；但要让修复生效，需要用新版本覆盖原来的安装目录并重新注册一次计划任务——双击 `一键安装.cmd` 即可，任务是同名覆盖，`run-hidden.vbs` 也会自动重写。
- 计划任务、配置项与默认值都没有变化，`drcom-config.json` 不用改。

## [1.5.0] - 2026-09-12

这一版针对「被踢下线」：根因是脚本在判定离线的瞬间又提交了一次登录，把正在使用的会话顶掉。现在登录侧加了三道闸门。

### 新增

- **登录前二次确认**（`LoginConfirmDelaySec`，默认 3 秒）：第一次查询显示离线后，等 3 秒再复检，**两次都离线才登录**；复检回到在线就按在线处理，不发登录请求。
- **最小登录间隔**（`LoginMinIntervalSeconds`，默认 60 秒）：两次 `/drcom/login` 之间硬性间隔 60 秒，网络抖动、开机触发、网络变化事件连续触发时也不会连打。
- **限流 / 冲突长冷却**（`LoginCooldownMinutes`，默认 30 分钟）：
  - Portal 明确限流（`error5 waitsec` / `Error code 205` / “请求过于频繁”）→ 立刻进入冷却；
  - 返回「账号已在别处在线」（`userid error2`）且本机复检仍不在线 → 判定为重复认证冲突，进入冷却。
  冷却期间只查询状态、不再登录（旧版只在单次运行内等 15 秒，30 秒后又会去撞）。
- 新增参数 `-LoginConfirmDelaySec`、`-LoginMinIntervalSeconds`、`-LoginCooldownMinutes`（`-1` 表示读配置）。
- `state.json` 新增 `CooldownUntil`、`CooldownReason`；诊断脚本新增一节【5】触发闸门与冷却，显示本地判断方式、最近一次真实查询时间、是否在冷却、冷却原因与剩余时间。

### 变更

- 连续失败退避与冷却合并：连续失败 3 次后的退避现在写进同一个冷却字段，取两者中较长的那个。
- 登录次数（`LoginWindowCount`）改为**只有真正提交登录时**才计数，二次确认后回到在线的运行不再占用配额。
- 日志顺序调整：先过完所有闸门再打印「准备自动登录」，避免出现“准备登录 → 本次不登录”这种前后矛盾的日志。

### 说明

- 只改脚本，不需要重新注册计划任务。
- 端到端验证（本地假 Portal，6 个场景）：能上网+刚查过 0 请求；能上网超时只查 1 次；复检转在线不发登录；连续离线两轮只有 1 次登录；明确限流后进入冷却；重复认证冲突进入冷却。

## [1.4.0] - 2026-09-12

这一版把触发条件改成「**连不上网才动手**」：平时不再反复查询 Portal，只在发现网络不通时才去自动登录。

### 新增

- **本地联网判断（不发任何请求）**：脚本每次被唤起时先用系统网络状态判断能不能上网，依次尝试
  `Microsoft.Windows.NetworkListManager` COM，其次 `Get-NetConnectionProfile`，两者都读不到时退化为兜底巡检。
  - 本机没有可用网络连接 → 直接退出；
  - Windows 认为现在能上网 → 说明校园网会话正常，直接退出，一个请求都不发；
  - 连不上网 → 才去查 Portal、必要时自动登录。
- `state.json` 新增 `Connectivity` 字段，记录本次用的是哪种判断方式（`nlm` / `cim` / `fallback`），便于排查。

### 变更

- `OnlineCheckIntervalSeconds` 的语义改为「**能上网时**的兜底巡检间隔」，默认从 120 秒改成 **300 秒**：
  - `300`（默认）：能上网时每 5 分钟查一次，约 288 次/天；
  - `0`：能上网时**完全不查**，只有连接不上网络时才自动登录；
  - 负数：每次触发都查（等价于旧行为）。
- Portal 不可达时的日志改为只在状态变化时记录一条，不再每 30 秒刷屏一句。

### 说明

- 只改脚本文件，**不需要重新注册计划任务**。
- 端到端验证（本地假 Portal）：能上网 + 刚查过 → 0 请求；连不上网 → 立刻查询并登录；
  能上网但超过兜底间隔 → 正常巡检；持续断网 → 每次触发都检查。

## [1.3.0] - 2026-09-12

这一版专门解决“请求太多被 Portal 风控”的问题：计划任务照旧每 30 秒触发，但真正发出去的请求大幅减少。

### 新增

- **在线时自动降频**：新增配置项 `OnlineCheckIntervalSeconds`（默认 120 秒）和参数 `-OnlineIntervalSeconds`。
  任务每 30 秒被唤起，但脚本会先读 `state.json`：上次刚确认在线、又没到间隔时**直接退出，一个请求都不发**。
  掉线、Portal 不可达、刚登录成功这几种情况仍然维持 30 秒快速检查，所以“掉线自动重新登录”不会变慢。
  在线稳定时查询量从约 2880 次/天降到约 720 次/天，把该值改成 `300` 可降到约 288 次/天。
- **登录请求限速**：新增配置项 `LoginHourlyLimit`（默认 12 次/小时）。最近 1 小时内的登录请求达到上限后，本次只记录日志并跳过登录、等下一个小时窗口，避免异常情况下反复撞 Portal 把账号撞进风控。
- `state.json` 新增字段：`LastProbe`、`LoginWindowStart`、`LoginWindowCount`。

### 变更

- 单次运行内的登录尝试次数默认从 3 次改成 **1 次**（`-RetryCount` 默认 1）。下一次检查就在 30 秒后，没必要在一次运行里连打三次；真正的防重试由原有的失败退避负责。
- 心跳日志现在会同时打印任务触发间隔和在线时的实际查询间隔，方便核对。
- 安装脚本输出增加一行“在线时查询：每 N 秒”。

### 说明

- 这些改动**不需要重新注册计划任务**：升级后让脚本文件更新（`git pull` 或解压新版本覆盖）即可，计划任务本身不用动。
- 想恢复成“每次触发都查询”，把 `OnlineCheckIntervalSeconds` 和 `LoginHourlyLimit` 都设成 `0`。

## [1.2.0] - 2026-09-12

这一版新增“暂停 / 恢复”入口，想临时停掉自动登录时双击一下就行，不用再翻“任务计划程序”。

### 新增

- `暂停-校园网自动登录.cmd`：双击即可暂停。把计划任务设为“已禁用”，不再自动检查、不再自动登录，自动申请管理员权限。
- `恢复-校园网自动登录.cmd`：双击即可恢复。重新启用计划任务，并在恢复后立刻触发一次检查，如果此时是掉线状态会马上登录。
- `CampusAutoLoginTaskState.ps1`：上面两个入口共用的实现，支持 `-Action Pause|Resume|Status`。
  `Status` 不需要管理员权限，会打印任务状态、检查间隔、最近运行时间与返回码、`state.json` 摘要和最近日志。

### 说明

- 暂停只是禁用计划任务：**凭据（DPAPI 加密）、日志、状态文件全部原样保留**，随时可以恢复；想彻底删除请用 `Uninstall-CampusAutoLoginTask.ps1`。
- 两个 `.cmd` 会自动弹出 UAC 授权窗口；如果被组策略拦下，也可以手动执行
  `powershell -ExecutionPolicy Bypass -File .\CampusAutoLoginTaskState.ps1 -Action Pause`（需要管理员权限）。
- 图形界面等效操作：任务计划程序里右键 `CampusAutoLogin` → “禁用” / “启用”。
- 这一版只增加文件，没有改变计划任务的注册写法，升级后**不需要**重新运行安装脚本。

### 变更

- 发布流程改为「推送 `v*` 标签即自动发布」：以后打一个 `vX.Y.Z` 标签并推送，就会自动生成带 zip 的正式 Release，不再需要改工作流文件来触发。

## [1.1.0] - 2026-09-12

这一版解决了“注册计划任务被 Windows 拒绝”和“脚本看起来根本没在跑”两个主要问题，检查频率改为每 30 秒。

### 修复

- **计划任务注册被系统拒绝**（`The task XML contains a value which is incorrectly formatted or out of range`）。Windows 的任务计划有两条硬性限制：重复间隔最小 1 分钟（`PT30S` 会被拒绝），表示“无限期重复”时必须省略 `<Duration>` 元素（`PT0S` 和 `TimeSpan.MaxValue` 都会被判为超出范围）。现在改为用**两条各 1 分钟、彼此错开 30 秒的触发器**实现 30 秒检查，并保留“补 30 天时长 → 退化成每分钟一次”的降级链。
- **注册完无法确认任务是否真的在跑**。安装脚本现在会读回已注册的触发器、核对真实生效的间隔，再手动启动任务并统计“计划任务自动触发”的次数（默认观察 100 秒）。
- **`-RateLimited $false` 被当成“开关已打开”**。PowerShell 的 `[switch]` 参数即使传 `$false` 也算“存在”，导致限流标记恒为真；已改为 `[bool]` 参数。
- **安装脚本静默失败**。旧版要求用户自己开一个管理员 PowerShell，而 `#Requires -RunAsAdministrator` 会让窗口一闪而过，结果是既没有任务也没有日志。现在统一走自动 UAC 提权。

### 新增

- `Diagnose-CampusAutoLogin.ps1`：一键生成诊断报告（运行环境、Portal 连通性、凭据、日志、计划任务、隐藏启动器），并打印每条触发器的间隔和实际生效的检查间隔。
- `一键安装.cmd`：双击即可安装，自动申请管理员权限。
- 运行状态文件 `state.json`（`RunCount`、`Online`、`LastResult`、`ConsecutiveFailures`、`LastHeartbeat` 等），可用来确认脚本确实在被反复执行。
- 新参数：`-Quiet`、`-CheckOnly`、`-Force`、`-Relogin`、`-DataDir`、`-RetryCount`、`-RetryDelaySec`、`-HeartbeatMinutes`。
- 连续失败退避：连续失败 3 次后按 2 → 30 分钟逐步拉长重试间隔。
- 互斥锁 + 任务级 `IgnoreNew`，避免上一次还没跑完就被下一次叠加。

### 变更

- 检查间隔默认 **每 30 秒**（可通过 `drcom-config.json` 的 `CheckIntervalSeconds` 调整，改完重新运行一次安装脚本）。
- Portal 不可达时只记录状态，不再盲目尝试登录。
- 强制绕过系统代理：内网 Portal 走直连。
- 凭据、日志、状态统一放在 `%LOCALAPPDATA%\CampusAutoLogin`（仍兼容脚本目录下的旧凭据文件，仓库目录可以随意移动）。
- 日志只在状态变化或每小时心跳时写入，不再每 30 秒刷一条。
- 安装自检的观察时长由 80 秒调整为 100 秒。

## [1.0.0] - 2026-09-09

首个公开版本。

### 新增

- `DrcomAutoLogin.ps1`：Dr.COM（哆点）Portal 自动登录，支持 `chkstatus` 在线检测、`login` 提交、`logout` 注销，并把 `userid error2`、`error5 waitsec` 之类的错误码翻译成中文。
- `Save-DrcomCredential.ps1`：用 Windows DPAPI 加密保存账号密码，不以明文落盘。
- `Install-CampusAutoLoginTask.ps1` / `Uninstall-CampusAutoLoginTask.ps1`：注册与卸载计划任务（登录后启动、网络变化触发）。
- `Setup-DrcomAutoLogin.ps1`：一键安装（保存凭据 + 注册任务）。
- `generic/`：给其他学校用的通用版脚本与配置示例。
- `docs/DRCOM-PROTOCOL.md`、`docs/TROUBLESHOOTING.md`：协议说明与常见问题排查。

## 版本对照

| 版本 | 提交 | 日期 |
| --- | --- | --- |
| 1.5.0 | `4bd677f` | 2026-09-12 |
| 1.4.0 | `d68360f` | 2026-09-12 |
| 1.3.0 | `fc34fd0` | 2026-09-12 |
| 1.2.0 | `d0b551c` | 2026-09-12 |
| 1.1.0 | `3c91cb9` | 2026-09-12 |
| 1.0.0 | `075140a` | 2026-09-09 |

1.0.0 与 1.1.0 之间的过程性提交（`5d58cec`、`b4538f0`、`c06986f`、`c30b53f`、`3a1c4a9`）已并入 1.1.0。

## 升级方法

```powershell
cd <你的仓库目录>
git pull
```

升级后必须重新运行一次安装脚本（双击 `一键安装.cmd`，或执行 `Install-CampusAutoLoginTask.ps1`）。
触发器写法、检查间隔、脚本路径有变化时，只替换文件是不会生效的——计划任务里保存的是注册那一刻的内容。
