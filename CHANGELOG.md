# 更新日志

本项目所有值得记录的改动都会写在这里。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，日期格式为 `YYYY-MM-DD`。

## [2.0.0-pre.10] - 2026-09-17

**预发布（Pre-release）**：专治「网络已经恢复、程序却还在睡觉」。本机日志实测
（2026-09-17 23:09:51 → 23:10:54，全程 63 秒）：3 秒二次确认 → 提交登录 → 复检阶梯 3 / 5 / 12 秒
（三轮全失败）→ 5 秒后的下一轮探测 → **33 秒盲等** → 才发现网络早已恢复。
也就是说这段「重连中」的时间里，账号其实已经能上网，只是没人去确认。

### 修复

- **最小间隔等待期内不再「睡满剩余间隔」（本次主要改动）**：被最小间隔闸门拦下后，
  工作线程过去会一直睡到 60 秒窗口结束——本机实测那段 33 秒里一次探测都没做。
  现在等待期内按约 5 秒的节奏**只做本地探测**（不发 `chkstatus`、不发 `login`），
  网络一恢复立刻确认在线，日志写「最小间隔等待期内检测到网络恢复（本地探测通过，未发 Portal 请求）」。
  等待期内的 Portal 请求由「每轮 1 次」降为 **0 次**：既更快，也更少打扰 Portal。
- **登录提交后的复检阶梯先立刻看一眼**：以前固定先睡 3 秒才做第一次复检，
  登录瞬间生效的情况要白等 3 秒。现在先做一次 **0 延迟的本地内容校验**（不发 Portal 请求），
  命中就立刻算成功（日志写「本地内容校验通过（首轮立刻命中）」——不写具体秒数，
  因为这一轮内部会并行复测两遍，真机上实测可能要 4 秒），之后仍按 3 / 5 / 12 秒复检。

### 新增

- 新增 6 条端到端断言（全部通过）：等待期内本地探测到恢复（`state` 变在线）、
  日志写明等待期恢复、等待期内不再发 Portal 查询、等待期恢复后仍然只提交 1 次登录、
  登录后 25~40 秒内确认恢复（老写法要等满 60 秒窗口）、登录后首轮立刻复检命中且 ≤ 2 秒确认恢复。
  假 Portal 新增 `late-content`（登录后 30 秒才通）与 `instant-content`（登录后立刻通）两个场景。

### 说明

- 风控三件套一律不动：登录前 3 秒二次确认、两次登录至少间隔 60 秒、每小时最多 12 次。
  「必须等第二次登录才能恢复」的场景仍然要等满最小间隔（这是有意的）；本次提速的是
  **确认恢复**这一段：网络能上网之后，程序最多约 8 秒内就会确认，不再空等。
- 无新增配置键，`ConfigVersion` 仍为 6，老配置不改写。等待期的探测节奏跟随
  `OfflineProbeSeconds`（默认 5 秒，内部下限 3 秒），只在「登录等待窗口 + 探测异常」时生效；
  正常在线时仍是每 20 秒探测、每 2 分钟会话核对。
- 等待期不查 Portal 的代价：这段时间（最长 60 秒）里不会做只读会话核对，
  但本地探测仍在跑，网络恢复与否照样能立刻发现。

## [2.0.0-pre.9] - 2026-09-17

**预发布（Pre-release）**：这一版专治「明明早就掉线了，程序却迟迟不动手」。
本机日志实测（634 条事件 / 45 次掉线样本）：**探测到离线 → 提交登录只要 3 秒**，
真正的瓶颈是**发现**掉线——中位 **166 秒**、最坏 410 秒。慢在两处：定时会话核对每 5 分钟才跑一次；
「疑似掉线核对」被 60 秒的内部下限卡住，再叠加「等下一个探测轮」。另外程序没有订阅系统网络变化事件，
网线 / WiFi 真断开时也只能等下一轮探测。

### 修复

- **发现掉线太慢（中位 166 秒 → 约 40~60 秒）**，三条一起改：
  - 定时会话核对默认 **300 → 120 秒**（`SessionCheckSeconds`）：这是「账号被踢下线」最主要的发现途径，
    老配置里等于 300 的会自动迁移，用户自定义过的值（例如 240 / 600）原样保留；
  - 「疑似掉线核对」的内部下限 **60 → 20 秒**：这条只影响**只读** `chkstatus` 的发起频率，与登录风控无关；
  - 新增**系统网络变化事件**订阅（`NetworkAvailabilityChanged` + `NetworkAddressChanged`）：
    网线插拔 / WiFi 切换 / IP 变化后 **1 秒内**唤醒复检（1 秒防抖，避免事件风暴连打），
    不用再干等下一个探测周期。事件**不绕过**任何风控闸门，只是让「发现」更快。
- **`--relogin` 半途退出（真机复现）**：命令行实现固定跑 25 秒，注销成功、等待 3 秒后就退出，
  登录请求根本没提交，账号停在「已注销」状态直到下一轮检查。现在按**评估编号**等到
  「调用之后开始的那一轮评估」跑完（`Busy = false`）再退出，最长 120 秒；超时会打印警告。
- **界面最后一行仍然被裁掉**：默认窗口高 840 时，展开「高级设置」后第 10 行「HTTP 探测超时」
  落在可视区之外（截图与 UI Automation 坐标确认），必须最大化窗口才能改。
  现在主体内容整体放进 `ScrollViewer`（日志卡片与底部按钮保持固定、不跟着滚）。

### 新增

- **会话核对区分来源**：定时巡检写「会话核对：…」，疑似掉线核对写「疑似掉线核对：…」，
  一眼就能看出这次是常规巡检、还是本机探测已经不对劲。
  `state.json` 新增 `LastSessionCheckKind`（`periodic` / `suspect`），
  仪表盘「运行统计 → 会话校验」、`--status`、`--diagnose` 都会显示来源。
- 新增 8 条端到端断言（全部 117 条通过）：旧默认 300 秒迁移为 120 且自定义值保留、迁移只动会话核对
  不动兜底巡检、新装默认 120 秒、疑似路径 34 秒内完成两次核对、启动时订阅网络变化事件、
  `--relogin` 注销后确实提交登录、日志区分「疑似掉线核对」与「会话核对」、主体内容被 `ScrollViewer` 包裹。

### 说明

- 登录风控一律不动：登录前 3 秒二次确认、两次登录至少间隔 60 秒、每小时最多 12 次。
- 只读的 `chkstatus` 随之增多（约 750 次/天，学校官方客户端本身也是分钟级轮询状态）；
  若日后发现异常，把「高级设置 → 会话校验间隔」调大或设为 `0`（关闭）即可。
- `state.json` 新增一个键（`LastSessionCheckKind`），其余字段不变；旧版本读到它会忽略，可随时回退。

## [2.0.0-pre.8] - 2026-09-17

**预发布（Pre-release）**：这一版来自对 pre.7 的一次外部代码审查（通读源码 + 重编译 + 跑测试 +
渲染界面），修掉 6 类真问题，并给发布链路加了硬门禁。

### 修复

- **高级设置最后一行叠印（pre.7 引入的界面回归）**：「残留会话自动重登」与「HTTP 探测超时」
  两行的标签与输入框整个叠在一起（`60` 和 `3000` 重合）。原因是那个网格只声明了 9 个
  `RowDefinition`，却用到了 `Grid.Row="9"`——WPF 对越界行号**不报错**，而是把控件塞进最后一行。
  现已补上第 10 行，并新增「界面 XAML 行列越界扫描」的常驻断言，这类错误以后会在测试里直接失败。
- **点「卸载」删不掉程序文件，界面却说已卸载**：正在运行的 exe 是删不掉的，旧实现照样弹
  「已卸载」，按钮随后又变回「卸载」。现在卸载后**程序自己退出**，由独立清理进程等本次进程
  退出后删除 exe 与空目录（最多等 240 秒），另外登记 `MoveFileEx(MOVEFILE_DELAY_UNTIL_REBOOT)`
  作为兜底；脚本用临时文件承载路径，不再把路径拼进 `cmd` 命令行。
  新增 `--uninstall --check-only`：只打印「会删什么、文件什么时候消失」，不删任何东西。
- **重试绕过 60 秒最小间隔**：`RetryCount` 调大后，一次登录流程会每 2 秒连打一次登录接口，
  完全绕过「两次登录至少间隔 60 秒」，与文档承诺的「不会反复顶号」不符。
  现在**每次提交前都重新过闸门**：距上次提交不足最小间隔就停止本轮重试，交给下一个周期；
  「账号已在别处在线 / 密码错误」这类提示在复检未恢复时**直接停止重试**（再打一次只会把
  刚建立的会话顶掉）。
- **每小时上限被悄悄放大**：`LoginWindowCount` 以前按「一次登录流程」+1，`RetryCount=3` 时
  3 次请求只算 1 次，12 次/小时实际上变成 36 次。现在**按真正提交出去的请求**计数，
  并且每次提交前都会重新检查上限。
- **用户操作失败时没有任何提示**：安装 / 卸载、开机自启、清理旧版残留的异常被全局兜底
  吞掉，只在 `crash.log` 留痕，界面上看起来「点了没反应」。现在这些动作各自 `try/catch` +
  弹窗说明原因（全局兜底继续负责后台线程）。
- **Portal 响应原文可能带着密码落盘**：登录失败时会把 Portal 的响应原文写进 `login.log`、
  `state.json` 和「复制诊断信息」的剪贴板。多数学校只回一句提示，但万一某校 Portal 把提交的
  表单回显出来，密码就跟着落盘了。现在日志与状态文件在写入前统一过一遍脱敏
  （`Redact`）：**密码一律 `***`，账号统一 `251******`**，`upass=…` 形态也会被打码。
- **守护会把正在登录的进程当「卡死」杀掉**：`StaleSeconds` 固定 180 秒，`RetryCount` 调大后
  一次登录流程本身就可能超过 180 秒。现在引擎在登录流程每轮提交前**强制刷新心跳**，
  守护的阈值也改为按配置动态计算（`max(180, 一轮评估预算 + 90)`，上限 900 秒），
  `--watchdog --check-only` 会打印当前生效的阈值。

### 新增

- **`en_md5` 明确提示**：协议文档里写了 Dr.COM 可能要求 `en_md5=1`（密码按 MD5 提交），
  本工具只按明文提交。这类学校会一直收到 `userid error2 / 密码错误`，现在日志里会额外
  用一行提示说明原因（限频一次），`docs/TROUBLESHOOTING.md` 也补了说明。
- **发布链路硬门禁**：构建脚本新增源码指纹 `dist/CampusNet.exe.src.sha256`
  （由 csproj + `src/**/*.cs` + `src/**/*.xaml` + 构建脚本算出，**排除 `bin/` 与 `obj/` 里的
  编译生成文件**——那里的 `App.g.cs`、`*.AssemblyInfo.cs` 带本机绝对路径，谁算谁不一样）。
  指纹还对路径与换行做了归一（正斜杠 + CRLF→LF），Windows 与 Linux 算出同一个值；
  `create-release` 的门禁单独跑在 Windows runner 上复核。`build-verify` 与 `create-release`
  在指纹不一致时**直接失败**，堵住「改了源码忘重建就发版」；二进制逐字节比对仍为警告，
  因为不同编译器版本会差几十字节（同样源码实测差 100 字节）。

### 测试

- 端到端断言从 84 条增至 96 条，新增：Portal 回显表单时的脱敏、`RetryCount>1` 不绕过最小间隔、
  每小时计数按请求累加、守护阈值随配置放宽、`--uninstall --check-only` 只打印不删除、
  界面 XAML 行列越界扫描。

## [2.0.0-pre.7] - 2026-09-17

**预发布（Pre-release）**：这一版来自对 pre.6 的一次逐行代码走查，修掉 10 个真问题。
其中 5 个直接影响「网络断了到底能不能自动回来」，另外几个是安全与健壮性。

### 修复

- **登录成功误判（最要命的一条）**：登录接口返回成功后，只要状态接口不可达就被记成
  `login-ok`（旧代码 `if (after.Online || !after.Reachable)`）。真实情形是
  「Portal 恰好掉线 + 其实没联上网」——程序以为已经好了，之后很长时间不再重试，
  网络就一直不恢复。现在**只有 Portal 明确回答「在线」才算成功**，状态接口不可达时
  记为**待确认**（`login-unconfirmed`，黄色），继续按异常节奏复检，且**不写**「登录成功」时间。
- **断网时判定太慢**：探测目标以前是**串行**等待的，三个 HTTP 目标每个最少 5 秒超时，
  单轮就要十几秒、加上复检接近 50 秒——「秒级重连」在超时型断网下根本不成立。
  现在**所有探测目标并行跑**（整轮耗时约等于单条目标的超时），HTTP 目标有独立的
  `HttpProbeTimeoutMs`（默认 3000 毫秒），复检默认由 3 轮 / 1000 毫秒收紧到
  **2 轮 / 500 毫秒**：完全断网到提交登录由几十秒降到十几秒。
- **探测目标写错会「永远在线」**：`ProbeTargets` 里一条合法目标都没有时（例如全写错格式），
  旧代码直接返回「在线」，自动登录永远不会触发。现在判为**配置错误**（`probe-config`，红色），
  停止自动登录并在日志里给出示例，既不冒充当在线，也不拿着坏配置去反复打 Portal。
- **配置损坏被静默忽略**：`config.json` 解析失败时旧代码直接退回**默认配置**继续跑，
  用户自定义的 Portal 地址一丢，账号密码就可能被发到默认地址去。现在文件损坏（含空文件、
  半截 JSON）一律**停止自动登录**并提示（`config-invalid`，红色），`--status` / `--diagnose`
  也会写明原因；损坏的配置文件不会被默认配置悄悄覆盖。
- **配置文件写入不再有「空窗期」**：旧实现先删旧文件再改名，中间崩溃就把配置整个丢了。
  现在写**唯一临时文件** → 先 `Flush(true)` 落盘 → `File.Replace` 原地替换（不支持时才退回删除 + 改名）。
- **界面保存设置与后台线程的竞态**：`Reload()` 以前整体替换配置 / 凭据 / 状态对象，
  而后台线程此时可能正拿着旧引用在跑：会出现「旧账号 + 新地址」、本轮状态写进被丢弃的对象、
  状态文件被旧内容覆盖。现在每一轮评估在开头把配置 / 凭据 / Portal 客户端抓成**同一套快照**，
  状态对象**永不替换**（改为原地刷新）。
- **守护进程的降速从来没生效**：达到快速崩溃上限后设的 300 秒节奏，下一轮循环又被重置回 30 秒，
  于是崩溃的程序一直按 30 秒被反复拉起。现在降速跨轮保留，主程序恢复正常后自动回到 30 秒。
- **守护进程丢失 `--data-dir`**：用自定义数据目录时，守护会按默认目录拉起主程序
  （读错的配置、错的账号）。现在拉起主程序时**原样透传** `--data-dir`，
  `--watchdog --check-only` 还会打印它将要使用的重启命令，便于核对。
- **Portal 请求的安全加固**：登录请求**禁止跟随跳转**（一个 307/308 就能把账号密码
  原样转发到别的地址），所有响应都校验**来源主机**，跨主机的一律丢弃；
  新增 `PortalScheme` 配置支持 `https`（`PortalHost` 也可以直接写成 `https://…`）。
- **命令行不再接受明文密码**：`--set-credentials <账号> <密码>` 会留在 PowerShell 历史、
  进程列表和审计日志里。现在**直接拒绝**（退出码 2）并提示改用 `--password-stdin`
  或图形界面；不带 `--password-stdin` 时会在控制台提示输入且不回显。

### 新增

- 命令行：`CampusNet.exe --set-credentials <账号> [--password-stdin]`。
- 配置项：`PortalScheme`（`http` / `https`）、`HttpProbeTimeoutMs`（默认 `3000`，范围 500–10000）。
- 高级设置新增「HTTP 探测超时（毫秒）」输入框，改完立即生效。
- 探测目标支持同时要求内容与状态码：`http:主机/路径|期望文本|204`
  （以前第二个竖线之后的写法会被整串当成关键字，等于「只认 204」从未生效）。
- 三个显式告警状态：`login-unconfirmed`（待确认）、`probe-config`、`config-invalid`。

### 变更

- `ConfigVersion` 升到 `5`：pre.6 的复检参数（3 轮 / 1000 毫秒）一次性迁移为 2 轮 / 500 毫秒，
  自定义过的数值原样保留。
- 默认值：`ConfirmAttempts` `3 → 2`、`ConfirmGapMs` `1000 → 500`、新增 `HttpProbeTimeoutMs=3000`。

### 测试

- 端到端断言由 68 条增加到 **84 条**，全部通过。新增覆盖：状态接口不可达不计成功、
  探测目标全非法、配置文件损坏、命令行密码被拒、复检参数迁移、守护自检的严格退出码
  （alive=0 / stale=1 / dead=2，不再「0 或 1 都算过」）。
- **并行探测的耗时对比**成为正式用例：4 个「连得上但永不回内容」的目标与 1 个相比，
  耗时差必须小于 1 秒（串行会差约 3.6 秒）。
- 假 Portal 现在能返回任意状态码（含**真正的 204 空响应**），
  以及「回完登录就立刻消失」的场景——「只认 204」这类用例首次被真正测到。

## [2.0.0-pre.6] - 2026-09-16

**预发布（Pre-release）**：这一版是围绕「后台根本没在干活」的一次大修。翻本机的
事件日志和崩溃转储之后找到了真凶：**托盘图标的一次超长文本把整个后台进程打崩了**，
而进程崩掉之后没有任何东西把它拉回来——于是账号被踢下线时，日志里什么都没有，
网络自然一直不恢复。下面每一条都是针对这个链条的。

### 修复

- **后台进程崩溃消失（真凶）**：托盘提示文本被截断到 120 字符，但 Windows 的上限是
  **63 字符**，超出就抛 `ArgumentOutOfRangeException`；这个异常发生在 WPF 事件回调里，
  直接让整个进程消失（事件日志里 6 次崩溃全是同一处）。现在统一按 63 字符做两轮降级，
  并且写入托盘、气泡提示都套上兜底，**再长也不会崩**。
- **崩溃后没人管**：新增**守护进程**——主程序启动时会拉起一个独立的小进程
  （`--watchdog-loop`），它每 30 秒确认一次「主程序还在、引擎心跳还新鲜」，
  不在就拉起来、卡死就结束再重启；主程序同时向 Windows 注册了「崩溃后立刻重启一次」。
  用户主动退出（托盘 → 退出 / 卸载）时守护会一起收工，不会跟你抢着启动。
  本机此前出现过 5 小时无人值守的日志断档，现在不会再有。
  （原先准备用计划任务实现，但本机账号的 UAC 令牌是「非提升」的，`schtasks` 直接拒绝访问；
  守护进程完全不需要管理员权限，效果一样。想额外加一层的话，
  可以用管理员 PowerShell 手工注册 TROUBLESHOOTING 里给的那条计划任务。）
- **「账号已在别处在线 / 密码错误」被当成登录失败**：按需求**完全忽略**这类 Portal
  业务提示——不计连续失败、不写「最近错误」，只记一行日志和「已忽略提示」计数；
  提示后仍会做几次轻量复检（+3/+5/+12 秒），一发现网络其实通了就立刻算成功。
  只有连不上登录接口、或收到完全无法识别的响应时才算真正的失败。
- **探测目标换掉最不稳的一条**：本机实测微软 `connecttest.txt` 单次失败率约 10%，
  会让状态在「在线 / 只有 TCP 握手」之间来回跳。新默认四条是
  `connect.rom.miui.com/generate_204`（只认 204）、百度 `robots.txt`（校验 `Baiduspider`）、
  微软 `connecttest.txt`、以及一个 TCP 兜底目标，实测连续 8 次全部稳定。
- **疑似掉线时复探太慢**：内容校验失败后本来要等满 20 秒才做第二次判定，现在改成按
  「异常」的节奏（默认 5 秒）复探，被踢下线后大约十几秒就能走完核对与登录。
- **残留会话挡路**：本机连续 60 秒（`StuckReloginSeconds`）探测不通、Portal 却说账号在线时，
  自动执行一次「注销 + 重新登录」，等同于手动点「立即重连」，不再需要你盯着。

### 新增

- 运行统计新增 **已忽略提示** 与 **强制重登** 两行；高级设置新增
  **残留会话自动重登（秒，0=关闭）**，改完立即生效。
- `crash.log`（崩溃现场，含完整堆栈）与 `watchdog.log`（守护自己的动作日志）。
- 命令行 `CampusNet.exe --watchdog [--check-only]`（一次性自检）与 `--watchdog-loop`（守护主循环），
  便于手动确认守护判定。
- 探测目标新增「只认状态码」写法：`http:主机/路径|204`。

### 变更

- `ConfigVersion` 升到 `4`：旧的三条默认探测列表会一次性迁移为新的四条；自定义过的探测列表原样保留。
- 自检模式改用独立的互斥体，托盘里已经有一个实例时也能跑 `--selftest`。

## [2.0.0-pre.5] - 2026-09-15

**预发布（Pre-release)**：真机检查 pre.4 的运行日志时发现「清空日志」有个小坑，顺手修掉。
网络相关逻辑与 pre.4 完全相同。

### 修复

- **清空日志**：如果 `login.log` 正被别的程序打开着（记事本、另一个实例、日志查看器……），
  Windows 会拒绝删除文件；旧实现会静默忽略这个失败，只补写一行「日志已清空」，
  于是看起来清空了、其实旧内容还在（本机日志里确实出现过这种现象）。
  现在删除失败会退而把文件**截断成空文件**，连截断也失败才写一条 `WARN` 说明是哪个文件没处理干净。

## [2.0.0-pre.4] - 2026-09-15

**预发布（Pre-release）**：pre.3 装到真机跑了十几分钟后发现的新问题——本机访问内容校验目标
`www.msftconnecttest.com/connecttest.txt` 会**偶尔超时**（实测 12 次里有 2 次卡到 3 秒超时，
平时只要 240–1600 ms）。这本来不影响安全（程序只会多查一次 Portal 状态、绝不会误登录），
但会让状态在「内容校验通过 / 只有 TCP 握手」之间来回跳，也白白多出一些只读请求。
这一版加上「第二次机会」补测，把这种抖动挡掉。

### 修复

- **内容校验补测（第二次机会）**：探测已经确认 TCP 通畅、只是内容校验没过时，**立刻再补测一次**
  内容校验目标；补测通过就直接按「在线」处理（0 个 Portal 请求）并写一行日志说明是抖动。
  只有补测也失败，才去核对 Portal 并走登录流程——所以「被踢下线」的发现速度不受影响
  （仍然当轮就会向 Portal 求证），单次超时却不会再打扰 Portal。
- 补测只针对「TCP 通、内容不通」这一种情况，网络彻底不通时不会重复等待，
  断网后仍然按原来的快节奏（每 5 秒）探测并尽快登录。

### 测试

- 复用 pre.3 的 33 条断言全部通过；真实网络连续观测确认：补测生效后在线期间日志里
  不再出现因单次超时而触发的 Portal 核对。

## [2.0.0-pre.3] - 2026-09-15

**预发布（Pre-release）**：修掉 2.0.0-pre.2 的一个**严重缺陷**——在本机这类「网关替任意 TCP 连接代答握手」的
网络里，程序会把「TCP 连得上」当成「能上网」，于是账号被踢下线后仍然显示在线、完全不触发登录，
只有手动点「立即重连」才有用。**pre.2 用户请尽快换到 pre.3。**

### 修复

- **探测加内容校验（关键修复）**：新增 `http:主机/路径|期望文本` 探测目标，必须 HTTP 2xx **且**
  响应体包含期望文本才算通过（3xx 一律算失败，直连不走系统代理、不跟随跳转）。
  默认探测目标改为「① 内容校验目标 ② 两个 TCP 兜底」，被网关代答时不再误判为在线。
- **判定逻辑**：配置了内容校验目标却没通过（TCP 通、内容不通）时，不再直接当成在线，
  而是先置为「正在核对网络状态」并立即向 Portal 求证一次；Portal 说离线就直接进入登录流程。
  若用户把探测目标全设成 `tcp:`，则保持旧行为，交给下面的会话校对兜底。
- **在线会话核对（新增）**：在线时默认每 `SessionCheckSeconds`（300 秒，0 = 关闭）只读查询一次
  `chkstatus`；Portal 说离线立即进入登录流程（仍受 3 秒二次确认、60 秒最小间隔、每小时 12 次上限约束），
  Portal 不可达则保持在线并记录原因。
- **延迟不再显示 0 ms**：延迟取「按配置顺序第一个成功目标」的 RTT，不再取所有目标里的最小值
  （代答目标的 0 ms 会把真实延迟盖掉）；探测明细里 RTT < 5 ms 的目标会标注「（本地代答）」。
- **忽略「账号已在别处在线」**：`userid error2` 不再按特殊冲突处理，删除 `login-conflict` 状态键；
  仍做一次 2 秒复检（真在线就算成功），否则记为一次普通失败、日志只提醒一次，按最小间隔继续重试。

### 新增 / 调整

- 状态键新增 `verifying`（正在核对网络状态）；`state.json` 新增 `LastSessionCheck` / `LastSessionResult`；
  `--status`、`--diagnose` 增加会话核对与「探测方式（内容校验 / 仅 TCP）」信息。
- **界面**：删除「保存高级设置」按钮，输入框改为**改完即自动保存**（失焦或回车生效，非法值自动回退并提示范围）；
  高级设置新增「会话校验间隔（秒）」；日志卡片新增 **清空日志** 按钮（二次确认，同时清掉 `login.log` 与 `login.log.old`）；
  新增命令行参数 `--clear-log`；网络卡片显示逐目标探测明细；运行统计新增「会话校验」与「探测方式」两行；
  「立即重连」点击后按钮禁用 10 秒并显示「正在重连…」，避免连点。
- **配置迁移 v2 → v3**：`ProbeTargets` 若恰好等于 pre.2 的默认三项（纯 TCP），自动替换为带内容校验的新默认；
  用户自定义过的探测列表原样保留。新增配置项 `SessionCheckSeconds`（默认 300，范围 0–3600）。

### 测试

- 端到端新增三个场景：内容校验失败时去核对 Portal 并完成登录（本次严重问题的复现与修复验证）、
  探测全通但 Portal 会话离线时按会话间隔自动登录、`--clear-log` 清空日志；
  `userid error2` 场景改为断言「普通失败 + 原因含『已在别处』+ 不再是 login-conflict」。
  共 33 条断言全部通过。

## [2.0.0-pre.2] - 2026-09-15

> ⚠️ **已知严重问题（请勿继续使用）：** 这一版只用 TCP 握手判断在线。在本机这类
> 「上游网关替任意 TCP 连接代答握手」的网络里，连不存在的地址也会显示 0 ms「连上」，
> 结果是**账号被踢下线后程序仍然显示在线、不会自动登录**，延迟也会一直显示 0 ms。
> 该问题已由 **2.0.0-pre.3** 修复（内容校验探测 + 在线会话核对），请改用 pre.3。

**预发布（Pre-release）**：按使用反馈去掉冷却机制、把在线探测加密到 20 秒，并做了一批内部健壮性优化。
命令行参数、凭据与日志格式都不变，已装 2.0.0-pre.1 的直接换掉 exe 即可。

### 变更

- **去掉冷却机制**：连续失败指数退避、以及「Portal 限流 / 重复认证冲突后冷却 30 分钟」全部移除；
  这两种情况只记录原因、本轮不再重试，仍然保留登录前 3 秒二次确认、两次登录最小间隔 60 秒、
  每小时最多登录 12 次这三道保险，不会再连环提交登录请求。
- **在线探测间隔 60 秒 → 20 秒**：网络正常时每 20 秒做一次本地探测（仍然完全不碰 Portal），
  掉线能更快被发现，恢复也更快。
- **配置自动迁移**：新增 `ConfigVersion`；旧配置里显式写着的 60 秒会在首次运行时自动升级为 20 秒，
  自定义过的值（例如 45、120）原样保留；已废弃的 `LoginCooldownMinutes` 会被移除。
- **状态键调整**：删除 `cooldown`，新增 `login-conflict`（重复认证冲突）；`state.json` 不再写
  `CooldownUntil` / `CooldownReason`。
- 新增配置项 `UpstreamProbeSeconds`（「本机不通但 Portal 显示在线」时的兜底巡检间隔，默认 300 秒）。

### 改进

- **`state.json` 写入节流**：状态没有实质变化时最多每 60 秒落盘一次；状态切换、登录尝试与结果、
  暂停 / 恢复、凭据变化仍然立即写入，减少常驻进程的磁盘写入。
- **网卡信息缓存 30 秒**，联网状态翻转、点「刷新」或「复制诊断信息」时立即失效重读，不再每轮枚举网卡。
- **后台异常记录完整堆栈**：内部异常现在会把 `异常堆栈：…` 的多行堆栈一并写进日志，便于排障。
- **修掉唤醒竞态**：手动「立即重连」「刷新」的唤醒信号不会再因为落在评估与等待之间而白等一个完整周期。
- **高级设置新增三项**：登录最小间隔（秒）、兜底巡检间隔（秒）、探测超时（毫秒），保存后立即生效。
- 仪表盘「运行统计」把「冷却」一列换成「下次探测」倒计时，能直接看出它还活着。

### 文档

- README、使用说明、故障排查同步改写：去掉「橙色 = 冷却中」的描述，补上限流与重复认证冲突的处理说明。

## [2.0.0-pre.1] - 2026-09-14

**预发布（Pre-release）**：2.0 是一次彻底重构——自动登录与监控面板合并成**一个单文件 exe**，
不再需要 PowerShell、不再注册计划任务。请先试用，确认无误后再发正式版。

### 新增

- **单文件 exe**：`CampusNet.exe` 约 110 KB，用 C# / WPF 编写，只需要 Windows 10/11 自带的
  .NET Framework 4.8，不必安装任何运行时或第三方组件；双击即用，全部功能都在里面。
- **常驻托盘 + 单窗口仪表盘**：托盘图标颜色跟随状态（绿=在线、黄=重连中、橙=冷却、红=异常、灰=已暂停），
  双击打开仪表盘：状态横幅、网络信息（IP／网关／DNS／网卡／延迟／丢包 + 延迟柱状图）、
  账号设置、运行统计、日志（WARN/ERROR 着色、可只看异常）、底部操作栏。
- **不用计划任务**：改为开机自启（HKCU `Run` 项）+ 常驻进程，全程不需要管理员权限、不弹 UAC。
- **内置安装 / 卸载**：一键复制到 `%LOCALAPPDATA%\Programs\CampusNet\`、创建开始菜单快捷方式、写自启项，
  卸载时反向清理（数据默认保留）。
- **一键清理旧版**：自动检测 1.x 残留（计划任务 `CampusAutoLogin`、`C:\CampusAutoLogin` 脚本目录），
  在界面上一键清理（只有这一步需要一次 UAC 确认），避免新旧两套程序互相顶号。
- **诊断信息一键复制**：把版本、路径、网络、配置、风控状态、日志尾部打包成文本，直接粘贴求助。
- **命令行排障参数**：`--status`、`--diagnose`、`--relogin`（立即重连）、`--install`、`--uninstall`、
  `--once`、`--selftest`、`--version`，便于自动化与排查。

### 改进（触发策略重新设计）

- **正常时几乎零请求**：只要本地探测（TCP 连接多个公网目标 + ICMP）能通，**完全不碰 Portal**，
  不查状态、不登录；Portal 请求只在真的探测不通时才会出现。
- **断网秒级响应**：探测失败后 1 秒内连试多次，确认断开才查 Portal 并登录；异常期间每 5 秒探测一次，
  恢复后自动回到每 60 秒的正常节奏。网卡变化、休眠唤醒、解锁都会立即触发一次评估。
- **风控闸门全部保留**：登录前二次确认、两次登录至少间隔 60 秒、每小时登录上限 12 次，
  限流（`error5 waitsec` / `Error code: 205`）与「账号已在别处在线」冲突立即冷却 30 分钟。
- **Portal 请求显式绕过系统代理**，不会被本机代理软件（Clash 等）劫持导致误判。
- **状态与日志文件统一带 UTF-8 BOM**，记事本 / PowerShell 打开不再乱码。

### 移除

- 全部 1.x PowerShell 脚本、`.cmd` 一键安装 / 暂停 / 恢复入口、`monitor/` 监控面板脚本、
  `generic/` 通用模板与 `drcom-config.json` 示例（配置改为程序自动生成在数据目录里）。
  1.x 的历史版本仍然可以在 Releases 里下载回退。

### 升级提示

- 从 1.x 升级：直接运行 2.0 的 exe，界面会提示检测到旧版残留，点「清理旧版残留」删掉旧任务与脚本即可；
  旧的账号密码不会自动迁移，请在 2.0 里重新输入一次。
- 数据目录变化：`%LOCALAPPDATA%\CampusAutoLogin`（旧）→ `%LOCALAPPDATA%\CampusNet`（新）。
- 2.0 不再使用计划任务；如果你之前用「暂停 / 恢复」.cmd 停过脚本，那两个文件也随旧版一起清理。

### 说明

- 发布附件只提供 `CampusNet.exe` 一个文件：源码在仓库 `main` 分支，使用说明见 `docs/使用说明.md`。
- 1.x 的收官版本 **1.9.0** 仍在 Releases 里，供继续使用 PowerShell 版的同学下载。

## [1.8.0-pre.1] - 2026-09-13

**预发布（Pre-release）**：监控面板从头重写成 WPF 浅色现代界面。自动登录脚本只有版本号变化，
行为与 1.7.0 完全一致，已经装好的同学不需要重新安装。

### 新增

- **全新界面**：面板改用 WPF 绘制（Windows 系统自带，仍然零第三方依赖）——圆角卡片、自绘标题栏
  （拖动、缩放、贴边、双击最大化都照常可用）、矢量徽标、统一的浅色状态配色。
- **延迟迷你折线图**：画在「延迟趋势」卡片里，每个探测目标一条线，丢包的位置标红点，
  右上角按颜色列出每个目标的当前延迟。
- **连通状态时间线**：最近 60 次刷新各占一格，颜色和顶部横幅一致，一眼看出这段时间是稳定还是断续。
- **窗口图标**：新增 `panel.ico`（16–256 多尺寸，程序绘制），任务栏与 Alt+Tab 里都能认出来。
- **高分屏优化**：启动时声明 Per-Monitor V2 DPI 感知，文字不再发虚；Windows 11 下窗口自动圆角，
  Windows 10 自动退回方角。
- **新增配置项**：`SparklinePoints`（默认 `60`，折线图保留多少个采样点）、
  `TimelinePoints`（默认 `60`，时间线保留多少格）。缺省或删掉这两个键都能跑。

### 说明

- 面板**仍然只读**：不创建、不修改任何文件，不注册任务，不常驻托盘；网络行为还是只有默认每 10 秒一次
  ICMP ping（网关 + Portal）和手动点「立即检查一次」时的一次 `chkstatus` 只读查询。
- 折线图与时间线的数据只存在内存里（面板一关就没了），所以「不写文件」这条约束没有被破坏。
- 面板自身版本 1.1.0 → 2.0.0；`DrcomAutoLogin.ps1` 只把 `ScriptVersion` 从 1.7.0 提到 1.8.0，逻辑不变。
- **为合并进主包做准备**：面板的文件布局与路径解析约定写进了 `monitor/README.md`。将来只要把
  `monitor\` 整个目录放进主包、在一键安装脚本末尾加一句可选的「是否打开监控面板」即可，
  面板代码不需要改动。这一版还没有真正合并，两个包仍然分开下载。
- 数据层的函数名、`-DumpOnce` 的输出格式和 `# ===================== 入口` 分界线都保持不变，
  原有的自动化测试脚本可以照常使用。

## [1.7.0] - 2026-09-13

## [1.7.0] - 2026-09-13

这一版给监控面板补上「网络」一栏：本机 IP、网卡、网关、DNS，以及到网关和 Portal 的**延迟与丢包**。
自动登录脚本本身行为不变，只跟着改版本号。

### 新增

- **网络信息**：本机 IPv4 地址、网卡名称与速率、默认网关、DNS 服务器、网络名称与类别（能取到就显示），
  有多块网卡（比如 VMware 虚拟网卡）时，非主用的那些会并列显示在「其它」里。
  数据全部来自系统自带的 .NET 网卡接口，**普通权限就能读**，不依赖需要管理员的 CIM 命令。
- **延迟与丢包**：默认每 10 秒向**默认网关**和 **Portal 主机**各发一次 ICMP ping，
  显示当前延迟，以及最近 10 次的平均／最小／最大延迟与丢包率。异步探测，不卡界面。
  网关不回应 ICMP 时显示「无响应」，不会误报成故障。
- **新增配置项**：`PingEnabled`（默认 `true`）、`PingIntervalSeconds`（`10`）、`PingTimeoutMs`（`1000`）、
  `PingTargets`（`"gateway,portal"`）、`LatencySamples`（`10`）、`NetworkInfoSeconds`（`20`，网卡信息缓存）。
  把 `PingEnabled` 设为 `false` 就完全不发 ICMP，网络区只保留 IP／网关／DNS。
- `-DumpOnce` 会先同步探一次延迟，所以命令行输出里也能看到 IP 与延迟；`-SelfTest` 同样会走一遍探测链路。

### 说明

- **为什么加 ping 不影响风控**：之前被踢下线的根因是脚本反复提交 `/drcom/login` 登录请求。
  面板发的 ICMP echo 属于系统层面的连通性探测，不走 HTTP、不碰登录/状态接口、不建立会话，
  和账号风控无关；不想让它发任何包，关掉 `PingEnabled` 即可。
- 面板自身版本 1.0.1 → 1.1.0；`DrcomAutoLogin.ps1` 只把 `ScriptVersion` 从 1.6.1 提到 1.7.0，
  逻辑与 1.6.1 完全一致，不需要重新安装计划任务。

## [1.6.1] - 2026-09-12

只修监控面板的提示，自动登录脚本行为不变（只跟着改版本号）。

### 修复

- **不再把「没权限读」误报成「文件损坏」**：面板如果以别的 Windows 用户（或受限权限）运行，
  读 `state.json` 会被系统直接拒绝；以前这种拒绝被当成解析失败，横幅显示「state.json 损坏」，
  容易让人以为文件坏了、去删了重建。现在会先探测数据目录能不能读，明确显示
  「读不到数据目录（权限不足）：请用安装自动登录时的那个 Windows 用户打开面板」。
- **数据目录里没有 `state.json` 时**，提示改为「数据目录里没有 state.json：自动登录可能没装好，
  或者计划任务从未成功运行过」，不再与「完全没装」混为一谈。

### 说明

- 面板自身版本 1.0.0 → 1.0.1；`DrcomAutoLogin.ps1` 只把 `ScriptVersion` 从 1.6.0 提到 1.6.1，
  逻辑与 1.6.0 完全一致，不需要重新安装计划任务。

## [1.6.0] - 2026-09-12

这一版新增一个**只读的桌面监控面板**（单独打包发布），并顺手修掉几个只有细看才发现的问题。

### 新增

- **监控面板（`monitor/`，单独打包成 `campus-network-monitor-v1.6.0.zip`）**：Windows PowerShell 5.1 + WinForms，
  零第三方依赖，双击即开。顶部横幅按优先级用颜色给结论（绿＝正常在线、黄＝正在重连、橙＝冷却中、
  红＝凭据问题或脚本没在跑、灰＝未检测到自动登录）；指标区显示最近触发/真实查询时间、在线状态、
  连续失败次数、冷却到点与原因、本地判断方式（`nlm`/`cim`/`fallback`）、计划任务状态与返回码、下次运行时间、
  `state.json` 修改时间、已安装脚本版本（低于 1.5.1 标黄提示升级）；日志区显示 `login.log` 最近 200 行
  （警告橙色、错误红色，可只看警告和错误）；统计最近 24 小时的登录次数、成功、失败、Portal 限流、
  重复认证冲突与掉线次数。
- **路径体检**：顺着「计划任务 → 隐藏启动器 → 主脚本」逐项检查文件与起始目录是否存在，
  任意一环缺失就红字提示**僵尸任务（返回码 0x8007010B）**——上次 `D:\campus-network-auto-login`
  被删掉之后任务静默失败的那种情况，现在打开面板一眼就能看见。
- **按钮**：立即检查一次（只发一次 `chkstatus` 查询，不登录、不写文件，点后禁用 5 秒）、刷新、
  打开数据目录、复制诊断信息（当前指标 + 最近 30 行日志进剪贴板）、关闭；界面每 5 秒自动刷新。
- 面板支持 `-DumpOnce`（不开窗口打印一次状态，方便脚本化检查）与 `-SelfTest`（建窗口刷新一次后退出）。

### 修复

- **`$host` 只读变量导致的静默失败**：面板解析 Portal 地址时用了 `$host` 作为局部变量名，
  而 `$Host` 在 PowerShell 里是只读的自动变量，赋值会抛错并被 `try/catch` 吞掉，结果永远退回到内置默认地址；
  参数名同样不能用 `$Host`（调用时会直接报错）。已改名，并加注释说明原因。
- **计划任务返回码显示**：返回码按 32 位无符号数解释，避免个别环境下读成负数而显示成
  `0xFFFFFFFF8007010B`；`0x8007010B` 直接标注为「起始目录不存在或被删除（僵尸任务）」。
- **日志顺序与统计口径**：先读轮转出去的 `login.log.old` 再读 `login.log`，保证时间顺序正确；
  「进入冷却」那一行不再重复计入限流/冲突次数。

### 说明

- 监控面板**只读**：不创建、不修改任何文件，不注册计划任务，不常驻托盘；除了手动点「立即检查一次」，
  不发任何网络请求，因此不会影响 v1.5.x 的登录风控策略。
- 面板与自动登录包**分开打包**，互不依赖，可以放在任意目录、普通权限运行；
  Portal 地址解析顺序为：主脚本目录的 `drcom-config.json` → 面板的 `monitor-config.json` → 内置默认值。
- 自动登录脚本本身只改了一个版本号字符串（`ScriptVersion` 1.5.1 → 1.6.0），行为与 v1.5.1 完全一致，
  不需要重新安装计划任务。

## [1.5.1] - 2026-09-12

这一版是 v1.5.0 的修补：修掉几处「看着正常、其实会误导人或者埋雷」的地方。功能与默认值都没有变化。

### 修复

- **安装自检不再误报失败**：v1.4.0 之后脚本在「能上网」时会直接退出、不写 `state.json`，而安装脚本是靠 `state.json` 里的 `RunCount` 增加来判断任务有没有真的跑起来。结果在联网正常的机器上，自检几乎必然报红色失败，还会把任务降级成直接调用 `powershell.exe`。现在同时看两条证据：`RunCount` 增加，**或者**计划任务自己的「最近一次运行时间」往后走，任意一条成立就算通过。
- **`state.json` 改为每次触发都刷新**：新增 `LastTrigger` 字段，`RunCount` 按触发次数累加。于是「脚本还活着吗」可以直接看 `state.json` 的修改时间（正常在 30 秒以内），不必再等下一次真实的 Portal 查询。注意这只写本地文件，**不产生任何网络请求**，不影响「能上网时零请求」的设计。
- **安装时提醒“脚本放在临时目录”**：如果主脚本位于桌面、下载或临时目录，安装脚本会明确警告——删掉那个文件夹之后，计划任务会变成每次启动都失败（返回码 `0x8007010B`）的死任务。这正是旧版 `D:\campus-network-auto-login` 被删掉之后实际发生的情况。
- **诊断报告会检查“任务指向的脚本还在不在”**：跟着隐藏启动器找到真正执行的主脚本，显示它是否存在、版本号，并在「起始目录不存在」「主脚本不存在」「脚本位于桌面/临时目录」时给出明确提示；`state.json` 超过 5 分钟没更新也会直接报警。
- **修正文档与诊断报告里的错别字**（“退化成都市按有网处理” → “退化为按有网处理”），README 里的“当前版本”从 1.1.0 更正为 1.5.1。
- **通用版本（`generic/`）默认 `RetryCount` 由 3 改为 1**：在一次运行里连打三次登录请求，正是 v1.1.0 被 Portal 限流、把正在用的会话顶掉的老毛病；下一次检查就在 1~2 分钟后，没有必要在一次运行里重试三次。

### 说明

- 不需要重新输入账号密码（`credential.xml` 与目录无关）；但要让修复生效，需要用新版本覆盖原来的安装目录并重新注册一次计划任务——双击 `一键安装.cmd` 即可，任务是同名覆盖，`run-hidden.vbs` 也会自动重写。
- 计划任务、配置项与默认值都没有变化，`drcom-config.json` 不用改。

## [1.5.0] - 2026-09-12

这一版针对「被踢下线」：根因是脚本在判定离线的瞬间又提交了一次登录，把正在使用的会话顶掉。现在登录侧加了三道闸门。

### 新增

- **登录前二次确认**（`LoginConfirmDelaySec`，默认 3 秒）：第一次查询显示离线后，等 3 秒再复检，**两次都离线才登录**；复检回到在线就按在线处理，不发登录请求。
- **最小登录间隔**（`LoginMinIntervalSeconds`，默认 60 秒）：两次 `/drcom/login` 之间硬性间隔 60 秒，网络抖动、开机触发、网络变化事件连续触发时也不会连打。
- **限流 / 冲突长冷却**（`LoginCooldownMinutes`，默认 30 分钟）：
  - Portal 明确限流（`error5 waitsec` / `Error code 205` / “请求过于频繁”）→ 立刻进入冷却；
  - 返回「账号已在别处在线」（`userid error2`）且本机复检仍不在线 → 判定为重复认证冲突，进入冷却。
  冷却期间只查询状态、不再登录（旧版只在单次运行内等 15 秒，30 秒后又会去撞）。
- 新增参数 `-LoginConfirmDelaySec`、`-LoginMinIntervalSeconds`、`-LoginCooldownMinutes`（`-1` 表示读配置）。
- `state.json` 新增 `CooldownUntil`、`CooldownReason`；诊断脚本新增一节【5】触发闸门与冷却，显示本地判断方式、最近一次真实查询时间、是否在冷却、冷却原因与剩余时间。

### 变更

- 连续失败退避与冷却合并：连续失败 3 次后的退避现在写进同一个冷却字段，取两者中较长的那个。
- 登录次数（`LoginWindowCount`）改为**只有真正提交登录时**才计数，二次确认后回到在线的运行不再占用配额。
- 日志顺序调整：先过完所有闸门再打印「准备自动登录」，避免出现“准备登录 → 本次不登录”这种前后矛盾的日志。

### 说明

- 只改脚本，不需要重新注册计划任务。
- 端到端验证（本地假 Portal，6 个场景）：能上网+刚查过 0 请求；能上网超时只查 1 次；复检转在线不发登录；连续离线两轮只有 1 次登录；明确限流后进入冷却；重复认证冲突进入冷却。

## [1.4.0] - 2026-09-12

这一版把触发条件改成「**连不上网才动手**」：平时不再反复查询 Portal，只在发现网络不通时才去自动登录。

### 新增

- **本地联网判断（不发任何请求）**：脚本每次被唤起时先用系统网络状态判断能不能上网，依次尝试
  `Microsoft.Windows.NetworkListManager` COM，其次 `Get-NetConnectionProfile`，两者都读不到时退化为兜底巡检。
  - 本机没有可用网络连接 → 直接退出；
  - Windows 认为现在能上网 → 说明校园网会话正常，直接退出，一个请求都不发；
  - 连不上网 → 才去查 Portal、必要时自动登录。
- `state.json` 新增 `Connectivity` 字段，记录本次用的是哪种判断方式（`nlm` / `cim` / `fallback`），便于排查。

### 变更

- `OnlineCheckIntervalSeconds` 的语义改为「**能上网时**的兜底巡检间隔」，默认从 120 秒改成 **300 秒**：
  - `300`（默认）：能上网时每 5 分钟查一次，约 288 次/天；
  - `0`：能上网时**完全不查**，只有连接不上网络时才自动登录；
  - 负数：每次触发都查（等价于旧行为）。
- Portal 不可达时的日志改为只在状态变化时记录一条，不再每 30 秒刷屏一句。

### 说明

- 只改脚本文件，**不需要重新注册计划任务**。
- 端到端验证（本地假 Portal）：能上网 + 刚查过 → 0 请求；连不上网 → 立刻查询并登录；
  能上网但超过兜底间隔 → 正常巡检；持续断网 → 每次触发都检查。

## [1.3.0] - 2026-09-12

这一版专门解决“请求太多被 Portal 风控”的问题：计划任务照旧每 30 秒触发，但真正发出去的请求大幅减少。

### 新增

- **在线时自动降频**：新增配置项 `OnlineCheckIntervalSeconds`（默认 120 秒）和参数 `-OnlineIntervalSeconds`。
  任务每 30 秒被唤起，但脚本会先读 `state.json`：上次刚确认在线、又没到间隔时**直接退出，一个请求都不发**。
  掉线、Portal 不可达、刚登录成功这几种情况仍然维持 30 秒快速检查，所以“掉线自动重新登录”不会变慢。
  在线稳定时查询量从约 2880 次/天降到约 720 次/天，把该值改成 `300` 可降到约 288 次/天。
- **登录请求限速**：新增配置项 `LoginHourlyLimit`（默认 12 次/小时）。最近 1 小时内的登录请求达到上限后，本次只记录日志并跳过登录、等下一个小时窗口，避免异常情况下反复撞 Portal 把账号撞进风控。
- `state.json` 新增字段：`LastProbe`、`LoginWindowStart`、`LoginWindowCount`。

### 变更

- 单次运行内的登录尝试次数默认从 3 次改成 **1 次**（`-RetryCount` 默认 1）。下一次检查就在 30 秒后，没必要在一次运行里连打三次；真正的防重试由原有的失败退避负责。
- 心跳日志现在会同时打印任务触发间隔和在线时的实际查询间隔，方便核对。
- 安装脚本输出增加一行“在线时查询：每 N 秒”。

### 说明

- 这些改动**不需要重新注册计划任务**：升级后让脚本文件更新（`git pull` 或解压新版本覆盖）即可，计划任务本身不用动。
- 想恢复成“每次触发都查询”，把 `OnlineCheckIntervalSeconds` 和 `LoginHourlyLimit` 都设成 `0`。

## [1.2.0] - 2026-09-12

这一版新增“暂停 / 恢复”入口，想临时停掉自动登录时双击一下就行，不用再翻“任务计划程序”。

### 新增

- `暂停-校园网自动登录.cmd`：双击即可暂停。把计划任务设为“已禁用”，不再自动检查、不再自动登录，自动申请管理员权限。
- `恢复-校园网自动登录.cmd`：双击即可恢复。重新启用计划任务，并在恢复后立刻触发一次检查，如果此时是掉线状态会马上登录。
- `CampusAutoLoginTaskState.ps1`：上面两个入口共用的实现，支持 `-Action Pause|Resume|Status`。
  `Status` 不需要管理员权限，会打印任务状态、检查间隔、最近运行时间与返回码、`state.json` 摘要和最近日志。

### 说明

- 暂停只是禁用计划任务：**凭据（DPAPI 加密）、日志、状态文件全部原样保留**，随时可以恢复；想彻底删除请用 `Uninstall-CampusAutoLoginTask.ps1`。
- 两个 `.cmd` 会自动弹出 UAC 授权窗口；如果被组策略拦下，也可以手动执行
  `powershell -ExecutionPolicy Bypass -File .\CampusAutoLoginTaskState.ps1 -Action Pause`（需要管理员权限）。
- 图形界面等效操作：任务计划程序里右键 `CampusAutoLogin` → “禁用” / “启用”。
- 这一版只增加文件，没有改变计划任务的注册写法，升级后**不需要**重新运行安装脚本。

### 变更

- 发布流程改为「推送 `v*` 标签即自动发布」：以后打一个 `vX.Y.Z` 标签并推送，就会自动生成带 zip 的正式 Release，不再需要改工作流文件来触发。

## [1.1.0] - 2026-09-12

这一版解决了“注册计划任务被 Windows 拒绝”和“脚本看起来根本没在跑”两个主要问题，检查频率改为每 30 秒。

### 修复

- **计划任务注册被系统拒绝**（`The task XML contains a value which is incorrectly formatted or out of range`）。Windows 的任务计划有两条硬性限制：重复间隔最小 1 分钟（`PT30S` 会被拒绝），表示“无限期重复”时必须省略 `<Duration>` 元素（`PT0S` 和 `TimeSpan.MaxValue` 都会被判为超出范围）。现在改为用**两条各 1 分钟、彼此错开 30 秒的触发器**实现 30 秒检查，并保留“补 30 天时长 → 退化成每分钟一次”的降级链。
- **注册完无法确认任务是否真的在跑**。安装脚本现在会读回已注册的触发器、核对真实生效的间隔，再手动启动任务并统计“计划任务自动触发”的次数（默认观察 100 秒）。
- **`-RateLimited $false` 被当成“开关已打开”**。PowerShell 的 `[switch]` 参数即使传 `$false` 也算“存在”，导致限流标记恒为真；已改为 `[bool]` 参数。
- **安装脚本静默失败**。旧版要求用户自己开一个管理员 PowerShell，而 `#Requires -RunAsAdministrator` 会让窗口一闪而过，结果是既没有任务也没有日志。现在统一走自动 UAC 提权。

### 新增

- `Diagnose-CampusAutoLogin.ps1`：一键生成诊断报告（运行环境、Portal 连通性、凭据、日志、计划任务、隐藏启动器），并打印每条触发器的间隔和实际生效的检查间隔。
- `一键安装.cmd`：双击即可安装，自动申请管理员权限。
- 运行状态文件 `state.json`（`RunCount`、`Online`、`LastResult`、`ConsecutiveFailures`、`LastHeartbeat` 等），可用来确认脚本确实在被反复执行。
- 新参数：`-Quiet`、`-CheckOnly`、`-Force`、`-Relogin`、`-DataDir`、`-RetryCount`、`-RetryDelaySec`、`-HeartbeatMinutes`。
- 连续失败退避：连续失败 3 次后按 2 → 30 分钟逐步拉长重试间隔。
- 互斥锁 + 任务级 `IgnoreNew`，避免上一次还没跑完就被下一次叠加。

### 变更

- 检查间隔默认 **每 30 秒**（可通过 `drcom-config.json` 的 `CheckIntervalSeconds` 调整，改完重新运行一次安装脚本）。
- Portal 不可达时只记录状态，不再盲目尝试登录。
- 强制绕过系统代理：内网 Portal 走直连。
- 凭据、日志、状态统一放在 `%LOCALAPPDATA%\CampusAutoLogin`（仍兼容脚本目录下的旧凭据文件，仓库目录可以随意移动）。
- 日志只在状态变化或每小时心跳时写入，不再每 30 秒刷一条。
- 安装自检的观察时长由 80 秒调整为 100 秒。

## [1.0.0] - 2026-09-09

首个公开版本。

### 新增

- `DrcomAutoLogin.ps1`：Dr.COM（哆点）Portal 自动登录，支持 `chkstatus` 在线检测、`login` 提交、`logout` 注销，并把 `userid error2`、`error5 waitsec` 之类的错误码翻译成中文。
- `Save-DrcomCredential.ps1`：用 Windows DPAPI 加密保存账号密码，不以明文落盘。
- `Install-CampusAutoLoginTask.ps1` / `Uninstall-CampusAutoLoginTask.ps1`：注册与卸载计划任务（登录后启动、网络变化触发）。
- `Setup-DrcomAutoLogin.ps1`：一键安装（保存凭据 + 注册任务）。
- `generic/`：给其他学校用的通用版脚本与配置示例。
- `docs/DRCOM-PROTOCOL.md`、`docs/TROUBLESHOOTING.md`：协议说明与常见问题排查。

## 版本对照

| 版本 | 提交 | 日期 |
| --- | --- | --- |
| 1.8.0-pre.1 | `da85649` | 2026-09-13 |
| 1.7.0 | `e9530c3` | 2026-09-13 |
| 1.6.1 | `0e73605` | 2026-09-12 |
| 1.6.0 | `9e2a03d` | 2026-09-12 |
| 1.5.1 | `cfeb69e` | 2026-09-12 |
| 1.5.0 | `4bd677f` | 2026-09-12 |
| 1.4.0 | `d68360f` | 2026-09-12 |
| 1.3.0 | `fc34fd0` | 2026-09-12 |
| 1.2.0 | `d0b551c` | 2026-09-12 |
| 1.1.0 | `3c91cb9` | 2026-09-12 |
| 1.0.0 | `075140a` | 2026-09-09 |

1.0.0 与 1.1.0 之间的过程性提交（`5d58cec`、`b4538f0`、`c06986f`、`c30b53f`、`3a1c4a9`）已并入 1.1.0。

## 升级方法

```powershell
cd <你的仓库目录>
git pull
```

升级后必须重新运行一次安装脚本（双击 `一键安装.cmd`，或执行 `Install-CampusAutoLoginTask.ps1`）。
触发器写法、检查间隔、脚本路径有变化时，只替换文件是不会生效的——计划任务里保存的是注册那一刻的内容。
